class Booking:

    def __init__(self, bid, panel, user, date_from, date_to, file_type, file_path):
        self.bid = bid
        self.panel = panel
        self.user = user
        self.date_from = date_from
        self.date_to = date_to
        self.file_type = file_type
        self.file_path = file_path

